# title="mega_launcher"
# category="Archive"
# description="No description provided."
# difficulty="Unknown"
# requires=()

# title="mega_launcher"
# title="mega_launcher"
# category="Archive"
# category="Archive"
# description="No description provided."
# description="No description provided."
# difficulty="Unknown"
# difficulty="Unknown"
# requires=()
# requires=()


# title="mega_launcher"
# title="mega_launcher"
# title="mega_launcher"
# title="mega_launcher"
# category="Archive"
# category="Archive"
# category="Archive"
# category="Archive"
# description="No description provided."
# description="No description provided."
# description="No description provided."
# description="No description provided."
# difficulty="Unknown"
# difficulty="Unknown"
# difficulty="Unknown"
# difficulty="Unknown"
# requires=()
# requires=()
# requires=()
# requires=()




# title="mega_launcher"
# title="mega_launcher"
# title="mega_launcher"
# title="mega_launcher"
# title="mega_launcher"
# title="mega_launcher"
# title="mega_launcher"
# title="mega_launcher"
# category="Archive"
# category="Archive"
# category="Archive"
# category="Archive"
# category="Archive"
# category="Archive"
# category="Archive"
# category="Archive"
# description="No description provided."
# description="No description provided."
# description="No description provided."
# description="No description provided."
# description="No description provided."
# description="No description provided."
# description="No description provided."
# description="No description provided."
# difficulty="Unknown"
# difficulty="Unknown"
# difficulty="Unknown"
# difficulty="Unknown"
# difficulty="Unknown"
# difficulty="Unknown"
# difficulty="Unknown"
# difficulty="Unknown"
# requires=()
# requires=()
# requires=()
# requires=()
# requires=()
# requires=()
# requires=()
# requires=()








#!/data/data/com.termux/files/usr/bin/bash
#!/data/data/com.termux/files/usr/bin/bash
#!/data/data/com.termux/files/usr/bin/bash
#!/data/data/com.termux/files/usr/bin/bash
#!/data/data/com.termux/files/usr/bin/bash
#!/data/data/com.termux/files/usr/bin/bash
#!/data/data/com.termux/files/usr/bin/bash
#!/data/data/com.termux/files/usr/bin/bash
#!/data/data/com.termux/files/usr/bin/bash
#!/data/data/com.termux/files/usr/bin/bash
#!/data/data/com.termux/files/usr/bin/bash
#!/data/data/com.termux/files/usr/bin/bash
#!/data/data/com.termux/files/usr/bin/bash
#!/data/data/com.termux/files/usr/bin/bash
#!/data/data/com.termux/files/usr/bin/bash
#!/data/data/com.termux/files/usr/bin/bash
















MODULE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
MODULE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
MODULE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
MODULE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
MODULE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
MODULE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
MODULE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
MODULE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
MODULE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
MODULE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
MODULE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
MODULE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
MODULE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
MODULE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
MODULE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
MODULE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
















while true; do
while true; do
while true; do
while true; do
while true; do
while true; do
while true; do
while true; do
while true; do
while true; do
while true; do
while true; do
while true; do
while true; do
while true; do
while true; do
  echo "========================================"
  echo "========================================"
  echo "========================================"
  echo "========================================"
  echo "========================================"
  echo "========================================"
  echo "========================================"
  echo "========================================"
  echo "========================================"
  echo "========================================"
  echo "========================================"
  echo "========================================"
  echo "========================================"
  echo "========================================"
  echo "========================================"
  echo "========================================"
  echo "        Termux Mega-Installer Menu      "
  echo "        Termux Mega-Installer Menu      "
  echo "        Termux Mega-Installer Menu      "
  echo "        Termux Mega-Installer Menu      "
  echo "        Termux Mega-Installer Menu      "
  echo "        Termux Mega-Installer Menu      "
  echo "        Termux Mega-Installer Menu      "
  echo "        Termux Mega-Installer Menu      "
  echo "        Termux Mega-Installer Menu      "
  echo "        Termux Mega-Installer Menu      "
  echo "        Termux Mega-Installer Menu      "
  echo "        Termux Mega-Installer Menu      "
  echo "        Termux Mega-Installer Menu      "
  echo "        Termux Mega-Installer Menu      "
  echo "        Termux Mega-Installer Menu      "
  echo "        Termux Mega-Installer Menu      "
  echo "========================================"
  echo "========================================"
  echo "========================================"
  echo "========================================"
  echo "========================================"
  echo "========================================"
  echo "========================================"
  echo "========================================"
  echo "========================================"
  echo "========================================"
  echo "========================================"
  echo "========================================"
  echo "========================================"
  echo "========================================"
  echo "========================================"
  echo "========================================"
  echo "0) Exit"
  echo "0) Exit"
  echo "0) Exit"
  echo "0) Exit"
  echo "0) Exit"
  echo "0) Exit"
  echo "0) Exit"
  echo "0) Exit"
  echo "0) Exit"
  echo "0) Exit"
  echo "0) Exit"
  echo "0) Exit"
  echo "0) Exit"
  echo "0) Exit"
  echo "0) Exit"
  echo "0) Exit"
  echo "1) Run install_all.sh (if present)"
  echo "1) Run install_all.sh (if present)"
  echo "1) Run install_all.sh (if present)"
  echo "1) Run install_all.sh (if present)"
  echo "1) Run install_all.sh (if present)"
  echo "1) Run install_all.sh (if present)"
  echo "1) Run install_all.sh (if present)"
  echo "1) Run install_all.sh (if present)"
  echo "1) Run install_all.sh (if present)"
  echo "1) Run install_all.sh (if present)"
  echo "1) Run install_all.sh (if present)"
  echo "1) Run install_all.sh (if present)"
  echo "1) Run install_all.sh (if present)"
  echo "1) Run install_all.sh (if present)"
  echo "1) Run install_all.sh (if present)"
  echo "1) Run install_all.sh (if present)"
  echo "----------------------------------------"
  echo "----------------------------------------"
  echo "----------------------------------------"
  echo "----------------------------------------"
  echo "----------------------------------------"
  echo "----------------------------------------"
  echo "----------------------------------------"
  echo "----------------------------------------"
  echo "----------------------------------------"
  echo "----------------------------------------"
  echo "----------------------------------------"
  echo "----------------------------------------"
  echo "----------------------------------------"
  echo "----------------------------------------"
  echo "----------------------------------------"
  echo "----------------------------------------"
















  i=2
  i=2
  i=2
  i=2
  i=2
  i=2
  i=2
  i=2
  i=2
  i=2
  i=2
  i=2
  i=2
  i=2
  i=2
  i=2
  declare -A MAP
  declare -A MAP
  declare -A MAP
  declare -A MAP
  declare -A MAP
  declare -A MAP
  declare -A MAP
  declare -A MAP
  declare -A MAP
  declare -A MAP
  declare -A MAP
  declare -A MAP
  declare -A MAP
  declare -A MAP
  declare -A MAP
  declare -A MAP
  for script in "$MODULE_DIR"/install_*.sh; do
  for script in "$MODULE_DIR"/install_*.sh; do
  for script in "$MODULE_DIR"/install_*.sh; do
  for script in "$MODULE_DIR"/install_*.sh; do
  for script in "$MODULE_DIR"/install_*.sh; do
  for script in "$MODULE_DIR"/install_*.sh; do
  for script in "$MODULE_DIR"/install_*.sh; do
  for script in "$MODULE_DIR"/install_*.sh; do
  for script in "$MODULE_DIR"/install_*.sh; do
  for script in "$MODULE_DIR"/install_*.sh; do
  for script in "$MODULE_DIR"/install_*.sh; do
  for script in "$MODULE_DIR"/install_*.sh; do
  for script in "$MODULE_DIR"/install_*.sh; do
  for script in "$MODULE_DIR"/install_*.sh; do
  for script in "$MODULE_DIR"/install_*.sh; do
  for script in "$MODULE_DIR"/install_*.sh; do
    base="$(basename "$script")"
    base="$(basename "$script")"
    base="$(basename "$script")"
    base="$(basename "$script")"
    base="$(basename "$script")"
    base="$(basename "$script")"
    base="$(basename "$script")"
    base="$(basename "$script")"
    base="$(basename "$script")"
    base="$(basename "$script")"
    base="$(basename "$script")"
    base="$(basename "$script")"
    base="$(basename "$script")"
    base="$(basename "$script")"
    base="$(basename "$script")"
    base="$(basename "$script")"
    if [ "$base" != "install_all.sh" ]; then
    if [ "$base" != "install_all.sh" ]; then
    if [ "$base" != "install_all.sh" ]; then
    if [ "$base" != "install_all.sh" ]; then
    if [ "$base" != "install_all.sh" ]; then
    if [ "$base" != "install_all.sh" ]; then
    if [ "$base" != "install_all.sh" ]; then
    if [ "$base" != "install_all.sh" ]; then
    if [ "$base" != "install_all.sh" ]; then
    if [ "$base" != "install_all.sh" ]; then
    if [ "$base" != "install_all.sh" ]; then
    if [ "$base" != "install_all.sh" ]; then
    if [ "$base" != "install_all.sh" ]; then
    if [ "$base" != "install_all.sh" ]; then
    if [ "$base" != "install_all.sh" ]; then
    if [ "$base" != "install_all.sh" ]; then
      echo "$i) $base"
      echo "$i) $base"
      echo "$i) $base"
      echo "$i) $base"
      echo "$i) $base"
      echo "$i) $base"
      echo "$i) $base"
      echo "$i) $base"
      echo "$i) $base"
      echo "$i) $base"
      echo "$i) $base"
      echo "$i) $base"
      echo "$i) $base"
      echo "$i) $base"
      echo "$i) $base"
      echo "$i) $base"
      MAP[$i]="$script"
      MAP[$i]="$script"
      MAP[$i]="$script"
      MAP[$i]="$script"
      MAP[$i]="$script"
      MAP[$i]="$script"
      MAP[$i]="$script"
      MAP[$i]="$script"
      MAP[$i]="$script"
      MAP[$i]="$script"
      MAP[$i]="$script"
      MAP[$i]="$script"
      MAP[$i]="$script"
      MAP[$i]="$script"
      MAP[$i]="$script"
      MAP[$i]="$script"
      i=$((i+1))
      i=$((i+1))
      i=$((i+1))
      i=$((i+1))
      i=$((i+1))
      i=$((i+1))
      i=$((i+1))
      i=$((i+1))
      i=$((i+1))
      i=$((i+1))
      i=$((i+1))
      i=$((i+1))
      i=$((i+1))
      i=$((i+1))
      i=$((i+1))
      i=$((i+1))
    fi
    fi
    fi
    fi
    fi
    fi
    fi
    fi
    fi
    fi
    fi
    fi
    fi
    fi
    fi
    fi
  done
  done
  done
  done
  done
  done
  done
  done
  done
  done
  done
  done
  done
  done
  done
  done
















  echo -n "Select an option: "
  echo -n "Select an option: "
  echo -n "Select an option: "
  echo -n "Select an option: "
  echo -n "Select an option: "
  echo -n "Select an option: "
  echo -n "Select an option: "
  echo -n "Select an option: "
  echo -n "Select an option: "
  echo -n "Select an option: "
  echo -n "Select an option: "
  echo -n "Select an option: "
  echo -n "Select an option: "
  echo -n "Select an option: "
  echo -n "Select an option: "
  echo -n "Select an option: "
  read -r choice
  read -r choice
  read -r choice
  read -r choice
  read -r choice
  read -r choice
  read -r choice
  read -r choice
  read -r choice
  read -r choice
  read -r choice
  read -r choice
  read -r choice
  read -r choice
  read -r choice
  read -r choice
















  if [ "$choice" = "0" ]; then
  if [ "$choice" = "0" ]; then
  if [ "$choice" = "0" ]; then
  if [ "$choice" = "0" ]; then
  if [ "$choice" = "0" ]; then
  if [ "$choice" = "0" ]; then
  if [ "$choice" = "0" ]; then
  if [ "$choice" = "0" ]; then
  if [ "$choice" = "0" ]; then
  if [ "$choice" = "0" ]; then
  if [ "$choice" = "0" ]; then
  if [ "$choice" = "0" ]; then
  if [ "$choice" = "0" ]; then
  if [ "$choice" = "0" ]; then
  if [ "$choice" = "0" ]; then
  if [ "$choice" = "0" ]; then
    echo "Exiting."
    echo "Exiting."
    echo "Exiting."
    echo "Exiting."
    echo "Exiting."
    echo "Exiting."
    echo "Exiting."
    echo "Exiting."
    echo "Exiting."
    echo "Exiting."
    echo "Exiting."
    echo "Exiting."
    echo "Exiting."
    echo "Exiting."
    echo "Exiting."
    echo "Exiting."
    exit 0
    exit 0
    exit 0
    exit 0
    exit 0
    exit 0
    exit 0
    exit 0
    exit 0
    exit 0
    exit 0
    exit 0
    exit 0
    exit 0
    exit 0
    exit 0
  elif [ "$choice" = "1" ] && [ -f "$MODULE_DIR/install_all.sh" ]; then
  elif [ "$choice" = "1" ] && [ -f "$MODULE_DIR/install_all.sh" ]; then
  elif [ "$choice" = "1" ] && [ -f "$MODULE_DIR/install_all.sh" ]; then
  elif [ "$choice" = "1" ] && [ -f "$MODULE_DIR/install_all.sh" ]; then
  elif [ "$choice" = "1" ] && [ -f "$MODULE_DIR/install_all.sh" ]; then
  elif [ "$choice" = "1" ] && [ -f "$MODULE_DIR/install_all.sh" ]; then
  elif [ "$choice" = "1" ] && [ -f "$MODULE_DIR/install_all.sh" ]; then
  elif [ "$choice" = "1" ] && [ -f "$MODULE_DIR/install_all.sh" ]; then
  elif [ "$choice" = "1" ] && [ -f "$MODULE_DIR/install_all.sh" ]; then
  elif [ "$choice" = "1" ] && [ -f "$MODULE_DIR/install_all.sh" ]; then
  elif [ "$choice" = "1" ] && [ -f "$MODULE_DIR/install_all.sh" ]; then
  elif [ "$choice" = "1" ] && [ -f "$MODULE_DIR/install_all.sh" ]; then
  elif [ "$choice" = "1" ] && [ -f "$MODULE_DIR/install_all.sh" ]; then
  elif [ "$choice" = "1" ] && [ -f "$MODULE_DIR/install_all.sh" ]; then
  elif [ "$choice" = "1" ] && [ -f "$MODULE_DIR/install_all.sh" ]; then
  elif [ "$choice" = "1" ] && [ -f "$MODULE_DIR/install_all.sh" ]; then
    bash "$MODULE_DIR/install_all.sh"
    bash "$MODULE_DIR/install_all.sh"
    bash "$MODULE_DIR/install_all.sh"
    bash "$MODULE_DIR/install_all.sh"
    bash "$MODULE_DIR/install_all.sh"
    bash "$MODULE_DIR/install_all.sh"
    bash "$MODULE_DIR/install_all.sh"
    bash "$MODULE_DIR/install_all.sh"
    bash "$MODULE_DIR/install_all.sh"
    bash "$MODULE_DIR/install_all.sh"
    bash "$MODULE_DIR/install_all.sh"
    bash "$MODULE_DIR/install_all.sh"
    bash "$MODULE_DIR/install_all.sh"
    bash "$MODULE_DIR/install_all.sh"
    bash "$MODULE_DIR/install_all.sh"
    bash "$MODULE_DIR/install_all.sh"
  elif [ -n "${MAP[$choice]}" ]; then
  elif [ -n "${MAP[$choice]}" ]; then
  elif [ -n "${MAP[$choice]}" ]; then
  elif [ -n "${MAP[$choice]}" ]; then
  elif [ -n "${MAP[$choice]}" ]; then
  elif [ -n "${MAP[$choice]}" ]; then
  elif [ -n "${MAP[$choice]}" ]; then
  elif [ -n "${MAP[$choice]}" ]; then
  elif [ -n "${MAP[$choice]}" ]; then
  elif [ -n "${MAP[$choice]}" ]; then
  elif [ -n "${MAP[$choice]}" ]; then
  elif [ -n "${MAP[$choice]}" ]; then
  elif [ -n "${MAP[$choice]}" ]; then
  elif [ -n "${MAP[$choice]}" ]; then
  elif [ -n "${MAP[$choice]}" ]; then
  elif [ -n "${MAP[$choice]}" ]; then
    bash "${MAP[$choice]}"
    bash "${MAP[$choice]}"
    bash "${MAP[$choice]}"
    bash "${MAP[$choice]}"
    bash "${MAP[$choice]}"
    bash "${MAP[$choice]}"
    bash "${MAP[$choice]}"
    bash "${MAP[$choice]}"
    bash "${MAP[$choice]}"
    bash "${MAP[$choice]}"
    bash "${MAP[$choice]}"
    bash "${MAP[$choice]}"
    bash "${MAP[$choice]}"
    bash "${MAP[$choice]}"
    bash "${MAP[$choice]}"
    bash "${MAP[$choice]}"
  else
  else
  else
  else
  else
  else
  else
  else
  else
  else
  else
  else
  else
  else
  else
  else
    echo "Invalid choice."
    echo "Invalid choice."
    echo "Invalid choice."
    echo "Invalid choice."
    echo "Invalid choice."
    echo "Invalid choice."
    echo "Invalid choice."
    echo "Invalid choice."
    echo "Invalid choice."
    echo "Invalid choice."
    echo "Invalid choice."
    echo "Invalid choice."
    echo "Invalid choice."
    echo "Invalid choice."
    echo "Invalid choice."
    echo "Invalid choice."
  fi
  fi
  fi
  fi
  fi
  fi
  fi
  fi
  fi
  fi
  fi
  fi
  fi
  fi
  fi
  fi
















  echo
  echo
  echo
  echo
  echo
  echo
  echo
  echo
  echo
  echo
  echo
  echo
  echo
  echo
  echo
  echo
done
done
done
done
done
done
done
done
done
done
done
done
done
done
done
done
